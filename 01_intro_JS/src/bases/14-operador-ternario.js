

let firstName
let lastName

//forma de checar si una variable es null
//console.log(`${firstName || 'No name'} ${lastName || 'No lastName'}`);

const isActive = false
//uso de operador ternario
const message = (isActive) ? 'Activo' : 'Inactivo'

console.log(message);

